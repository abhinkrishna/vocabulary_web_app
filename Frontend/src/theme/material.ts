import { createTheme } from "@mui/material";

const appTheme = createTheme({
    palette: {
        primary: {
            main: '#5C1349',
        },
        secondary: {
            main: '#FFFFFF',
        },
        background: {
            default: '#5C1349',
        }
    }
})

export default appTheme;